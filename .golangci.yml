run:
  timeout: 5m
  tests: true
linters:
  enable:
    - gofmt
    - goimports
    - revive
issues:
  exclude-use-default: false

linters-settings:
  gofmt:
    simplify: true
  goimports:
    local-prefixes: github.com/flyingrobots/hubless
  revive:
    ignore-generated-header: true
